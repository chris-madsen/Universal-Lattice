# Result

Status: active

## Summary

Focused refinement is now in place for the strongest hybrid classes, and a later pure `axis+long` push strengthened the branch substantially. The old focused leader `A4_L6_H0` was weak, but the dedicated `A/L/H0` sweep found a much stronger bridge-class: `A2_L4_H0` with family5 hit rate `0.1456`.

## Best Witness

- `data/search_hybrid_sampling.json`
- `data/analyze_hybrid_refine.json`
- `data/analyze_axis_long_push.json`

## Blocking Issues

The hybrid branch now clearly points toward an `axis+long` scaffold. The next question is no longer whether that direction exists, but how aggressively it should be handed off and encoded inside branch `05`.
