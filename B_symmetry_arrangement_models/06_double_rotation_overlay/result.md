# Result

Status: sampled_red_flag

## Summary

The branch was reopened via stratified class sampling instead of being dropped on coarse complexity grounds. That sampling now gives a real negative signal: in 1200 sampled combinations there were 0 hits for the target family-count 5, and the best result still had family-count 14.

## Best Witness

- `data/search_stratified_sampling_report.json`
- `data/analyze_stratified_classes.json`

## Blocking Issues

As currently defined, the simple edge-union double-rotation overlay looks weak. Any future rescue would likely need a different readout or a nontrivial selection mechanism.
