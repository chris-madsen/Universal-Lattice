# 06_double_rotation_overlay

## Hypothesis
Current lattice comes from overlaying a 24-cell with a rotated copy under a general double rotation `(alpha, beta)`.

## Why It Is Plausible
Two natural webs can superpose into a web that looks much less natural in isolation.

## Witness
A concrete `(alpha, beta)` pair and overlay rule yielding a strong structural match.

## Kill Criteria
After pruning, the parameter search remains too large without any principled narrowing.

## Stages
- baseline
- prune
- search
- analyze
- finalize
